language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-linkfb' ]; then cd .. && eval "mv $currentfolder generator-linkfb" && cd generator-linkfb; fi

